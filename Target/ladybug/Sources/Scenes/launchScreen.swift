import SwiftUI

struct launchScreenView: View {
    var body: some View {
        Image("Image")
            .resizable()
            .frame(width: 200, height: 200)
            .shadow(radius: 15)
        
    }
}
